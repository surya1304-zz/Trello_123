import styled from "styled-components";

const Card = styled.div`
   {
    background: white;
    border: 1px solid #ccc;
    box-shadow: 0 0 5px #ccc;
    width: fit-content;
    padding: 10px;
    margin: 10px;
    position: relative;
  }
`;

export default Card;
